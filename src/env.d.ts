/// <reference types="astro/client" />

// globalThis and Window
declare global {
  // function myFunction(): boolean;
}
// just Window
interface Window {
  // var myString: string;
  // myFunction(): boolean;
}

export {};
